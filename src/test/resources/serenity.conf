webdriver {
    driver = chrome
    autodownload = true
}

headless.mode = false

serenity {
    project.name = "CyberGuard - Screenplay"
    take.screenshots = AFTER_EACH_STEP
}

environments {
    default {
        webdriver.base.url = "http://localhost:4200"
    }
}
